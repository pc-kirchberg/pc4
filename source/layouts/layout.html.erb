<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv='X-UA-Compatible' content='IE=edge;chrome=1' />
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>PC<%= ' - ' + current_article.title unless current_article.nil? %></title>
    <%= feed_tag :atom, "#{blog.options.prefix.to_s}/feed.xml", title: "Atom Feed" %>
    <%= stylesheet_link_tag "site" %>
    <%= yield_content :styles %>
  </head>
  <body class="<%= yield_content :body_class %>">

  <nav class="navbar">
      <button class="menu-button navlink">Menu</button>
      <a href="/" class="navlink">Home</a>
      <a href="/news.html" class="navlink">News</a>
      <ul class="navmenu">
        <li><a class="navlink" href="/pages/meet-the-committee/2019-2020.html">Meet The Committee</a></li>
        <li><a class="navlink" href="/pages/meet-the-committee/2019-2020.html">2019-2020</a></li>
        <li><a class="navlink" href="/">2018-2019</a></li>
      </ul>
      <a href="/pages/contact-us.html" class="navlink">Contact Us</a>
  </nav>

    <main id="main" role="main">
      <%= yield %>

      <aside>
      <h2>Recent Articles</h2>
      <ol>
        <% blog.articles[0...10].each do |article| %>
          <li><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></li>
        <% end %>
      </ol>

      <h2>Tags</h2>
      <ol>
        <% blog.tags.each do |tag, articles| %>
          <li><%= link_to "#{tag} (#{articles.size})", tag_path(tag) %></li>
        <% end %>
      </ol>

      <h2>By Year</h2>
      <ol>
        <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
          <li><%= link_to "#{year} (#{articles.size})", blog_year_path(year) %></li>
        <% end %>
      </ol>
    </aside>
    </main>
    <footer>
      Copyright &copy; 2019-forever the Pupils' Committee of the European School Luxembourg 1. All rights reserved, except for when they aren't. If you have to ask, they are.
    </footer>
    <%= javascript_include_tag "site" %>
    <%= yield_content :scripts %>
  </body>
</html>
